# RetailSense 部署手册（Streamlit Community Cloud）

代码侧已经配置完成：入口为 `app.py`，依赖已写入 `requirements.txt`。全程免费，不需要 API Key 或付费服务。

1. 打开 [Streamlit Community Cloud](https://share.streamlit.io/)，用 GitHub 账号注册或登录。
2. 按页面提示授权 Streamlit 读取 GitHub 仓库。
3. 点击 **Create app**（有些页面显示为 **New app**）。
4. 在仓库选择中填写或选择 `nancy-yin/retail-sense`。
5. Branch 选择 `main`，Main file path 填写 `app.py`。
6. 不需要填写 Secrets，也不需要改 Advanced settings，直接点击 **Deploy**。
7. 等待构建完成，打开生成的 `https://...streamlit.app` 链接；确认首页能进入“工作台”。
8. 把该链接补到 GitHub 主页、两份简历和作品集即可。

## 云端版与本地版的差异

- 云端是公开的**全功能内存演示**：自动以“演示管理员”进入，可查看配置面板并完成模拟配货、上架和评分默认值调整，不创建共享账号。
- 云端的配货、模拟上架、平台配置和产品图片操作只保存在当前浏览器 Session 的内存中；刷新、断开或云端重启后会恢复演示初始状态。请不要在公网演示中填写真实 API Key。
- 本地从 Git 仓库运行时，仍保留原本 `.auth/` 下的 JSON 持久化；如需在非 Git 副本中强制使用本地持久化，可设置 `RETAILSENSE_PERSISTENCE=local`。
