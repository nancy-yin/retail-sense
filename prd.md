# RetailSense 前端 UI 改版 PRD（Stitch 原型设计版）

> 文档用途：直接提供给 Stitch，用于生成 RetailSense 完整前端产品原型。
> 设计重点：页面结构、功能模块、状态反馈、角色权限和响应式体验。
> 本阶段不输出代码，不修改现有业务逻辑，不增加未经确认的新功能。

---

## 0. Stitch 执行说明

### 0.1 设计任务

请为 RetailSense 设计一套完整、可演示的 Web SaaS 原型，覆盖登录、工作台、案例库、选品评分、定价、销售自动化、库存、商品上架、物流配发、报表导出和设置。

原型应首先保证功能模块完整和业务路径清晰，再应用用户提供的参考视觉风格。不要为了视觉效果删除表格、输入项、状态提示、权限说明或业务边界说明。

### 0.2 设计优先级

1. 本 PRD 中的页面结构和功能要求。
2. 用户另外提供给 Stitch 的参考风格图片。
3. 本 PRD 中的视觉关键词。
4. 当前 RetailSense 的宠物图片和插图资产。

参考图只用于视觉语言，不得改变本 PRD 定义的功能范围和页面信息架构。

### 0.3 必须遵守的事实边界

- 三家公司、经营指标、案例评价和交易数据均为虚拟演示数据。
- 商品上架是模拟流程，只生成 Listing 和本地操作记录，没有调用真实平台 API。
- 物流订单、运单号和六阶段轨迹均为模拟数据。
- 货币换算使用内置演示汇率，不是实时汇率。
- Scout、Price、Copy、Monitor 当前是规则和模板流水线，不是调用大模型的自治 Agent。
- 当前角色是管理员 `admin` 与员工 `user`，不是生产级多租户 RBAC。
- 不能在任何原型页面出现默认管理员密码、真实密钥或真实客户暗示。

### 0.4 原型尺寸

- 桌面端主稿：1440 × 1024。
- 平板检查稿：834 × 1194。
- 手机端主稿：390 × 844。
- 桌面内容区建议最大宽度：1200–1280px。
- 需要保留可滚动页面的完整纵向结构，不要为了单屏展示压缩所有内容。

---

## 1. 文档信息

| 项目 | 内容 |
|---|---|
| 产品名称 | RetailSense |
| 当前产品版本 | v2.5.1 |
| 文档版本 | UI Redesign PRD v1.0 |
| 产品类型 | 零售运营决策 Web 原型 |
| 主要行业场景 | 宠物用品、跨境电商、小型零售团队 |
| 主要终端 | 桌面浏览器，兼容平板和手机 |
| 支持语言 | 中文、英文 |
| 原型目标 | 展示完整功能模块和 10 分钟面试演示闭环 |

---

## 2. 产品背景

RetailSense 将零售 SKU 管理经验拆成可解释的规则和操作流程，覆盖：

1. 选什么商品：四维选品评分。
2. 卖多少钱：完整成本和利润率红线计算。
3. 何时补货：安全库存、补货点和周转判断。
4. 如何形成运营闭环：内容生成、模拟上架、模拟配货与数据导出。

当前产品功能已经完整，但页面较多，视觉层级、组件一致性和移动端体验需要重新设计。本次改版要让用户快速识别“数据概况—决策—执行—复盘”的主线，同时保留宠物行业的亲和力。

---

## 3. 改版目标与成功标准

### 3.1 产品目标

- 用户进入工作台后 10 秒内理解当前数据源、营收和库存风险。
- 用户能在 3 次以内页面跳转完成“选品评分—定价—库存检查”。
- 面试官能在 10 分钟内理解产品价值、核心逻辑和实现边界。
- 所有主要页面使用统一的标题、卡片、表格、表单、状态标签和反馈模式。
- 保留全部现有宠物商品图、横幅和插图，不覆盖源文件。
- 桌面、平板和手机均能完成核心任务。

### 3.2 原型验收指标

- 11 个主要界面全部产出桌面稿。
- 7 个关键界面产出手机稿。
- 关键操作具备默认、加载、成功、失败、空状态或权限状态。
- 所有模拟能力都有明确标签。
- 所有风险状态同时使用文字、图标和颜色，不只依赖颜色。
- 原型页面之间可点击跳转，能完成本文定义的 4 条关键流程。

### 3.3 非本次范围

- 不设计真实支付、真实订单接入或真实平台授权流程。
- 不设计数据库后台、开发者控制台或复杂组织管理。
- 不调整评分、定价、利润、库存等计算公式。
- 不新增聊天机器人、预测模型或大模型配置页面。
- 不替换现有宠物图片，不生成风格不一致的新商品图。

---

## 4. 目标用户与权限

### 4.1 管理员／店主

核心诉求：掌握经营与库存情况，调整商品输入，管理数据源、图片和平台配置。

可执行：

- 查看所有通用页面。
- 切换虚拟公司数据。
- 编辑评分商品。
- 上传或恢复商品图片。
- 保存平台与物流配置。

不可执行：

- 商品模拟上架。
- 物流配货和虚拟发货。

设计要求：管理员进入员工专属页面时显示权限说明，而不是隐藏页面入口后让用户无法理解功能范围。

### 4.2 员工／运营人员

核心诉求：完成日常商品上架、订单配货、库存查看和报表导出。

可执行：

- 查看工作台、案例、评分、定价、流水线、库存和导出。
- 执行商品模拟上架。
- 执行物流配货和虚拟发货。

不可执行：

- 修改公司数据源、商品基础信息、图片和平台配置。

### 4.3 面试官／访客视角

不是系统内独立角色，但原型必须支持演示：

- 迅速理解产品定位。
- 看懂四维评分、完整成本和库存公式。
- 明确哪些功能已实现，哪些只是模拟。
- 能从任意核心页面返回工作台或进入下一演示步骤。

### 4.4 权限矩阵

| 页面／操作 | 管理员 | 员工 |
|---|:---:|:---:|
| 登录、工作台、案例库 | ✓ | ✓ |
| 选品评分与查看排名 | ✓ | ✓ |
| 编辑评分商品 | ✓ | — |
| 定价、销售自动化、库存 | ✓ | ✓ |
| 商品模拟上架 | — | ✓ |
| 物流配发与虚拟发货 | — | ✓ |
| 报表查看与导出 | ✓ | ✓ |
| 公司、图片、平台配置 | ✓ | — |

---

## 5. 核心使用场景

### 场景 A：新品决策

用户在工作台发现需要增加新品，进入选品评分，选择目标市场并完成四维评分；查看排名后进入定价页，确认建议售价与利润红线。

### 场景 B：库存风险处理

用户从工作台点击低库存卡片，进入库存监控并自动筛选风险商品；查看安全库存、补货点与建议补货量，判断优先补货 SKU。

### 场景 C：员工执行上架与履约

员工进入商品上架，选择商品和平台，确认 Listing 后完成模拟上架；随后进入物流页，为虚拟订单配货并查看六阶段物流轨迹。

### 场景 D：面试演示

演示者按“工作台—选品—定价—销售流水线—库存”的顺序讲解，在案例库说明虚拟业务场景，在关于或页面提示中说明系统边界。

---

## 6. 信息架构

### 6.1 主导航顺序

1. 工作台
2. 案例库
3. 选品评分
4. 定价模型
5. 销售自动化
6. 库存监控
7. 商品上架
8. 物流配发
9. 导出报表

### 6.2 导航分组建议

为了降低认知负担，视觉上分为四组，但保留原页面名称：

- 概览：工作台、案例库。
- 决策：选品评分、定价模型、销售自动化。
- 执行：库存监控、商品上架、物流配发。
- 数据：导出报表。

侧边栏底部放置：设置、关于、更新日志、版本号、退出登录。

### 6.3 页面层级

```text
登录／注册
└── 应用主框架
    ├── 工作台
    ├── 案例库
    ├── 选品评分
    ├── 定价模型
    ├── 销售自动化
    ├── 库存监控
    ├── 商品上架
    │   ├── 上架操作
    │   └── 上架记录
    ├── 物流配发
    │   ├── 订单列表
    │   └── 订单履约详情
    ├── 导出报表
    └── 设置
        ├── 显示与语言
        ├── 虚拟公司数据
        ├── 图片管理
        └── 平台配置
```

---

## 7. 全局框架要求

### 7.1 桌面端应用框架

左侧固定导航宽度建议 232–256px，包含：

1. RetailSense 品牌标识。
2. 当前用户名和角色标签。
3. 分组导航。
4. 设置、关于和更新日志。
5. 版本号 `v2.5.1`。
6. 退出登录。

主内容区包含：

1. 面包屑，仅二级页面或详情状态使用。
2. 页面标题、说明、数据源和页面级标签。
3. 右上角主要操作。
4. 页面主体。

### 7.2 顶部信息原则

每页标题区最多包含：

- 页面名称。
- 一句话用途说明。
- 当前虚拟公司／示例数据来源。
- “虚拟数据”“模拟流程”“规则引擎”等必要标签。
- 一个主要操作按钮。

避免在标题区堆放超过两个次要按钮。

### 7.3 全局组件

- 主按钮、次按钮、文字按钮、危险按钮。
- KPI 卡片、商品卡片、案例卡片、流程步骤卡片。
- 输入框、数字输入、选择器、分段控件、滑杆、复选框。
- 数据表格、筛选条、分页器。
- 状态标签：正常、建议补货、低库存、断货、待处理、处理中、已完成。
- Toast、表单错误、页面提示条、确认弹窗。
- 骨架屏、空状态、错误状态、无权限状态。

所有页面必须复用同一组件语言，不允许每个页面重新发明按钮和卡片样式。

---

## 8. 视觉与图片资产要求

### 8.1 视觉关键词

- 温暖、清爽、现代。
- 宠物友好但不幼稚。
- 轻量 SaaS、数据清晰。
- 柔和中性色背景。
- 少量品牌强调色。
- 适度圆角、克制阴影、充足留白。
- 插图负责情绪，数据负责决策。

### 8.2 当前必须保留的图片

品牌与场景图：

- `images/banner.jpg`
- `images/footer.jpg`
- `images/sidebar.jpg`
- `images/sidebar_dog.jpg`

商品图：

- `images/products/acrylic-tag.jpg`
- `images/products/bow-tie.jpg`
- `images/products/bracelet.jpg`
- `images/products/dog-tag.jpg`
- `images/products/enamel-plate.jpg`
- `images/products/leash-set.jpg`
- `images/products/led-collar.jpg`
- `images/products/treats.jpg`

### 8.3 图片使用规范

- 不删除、不覆盖、不改变源文件。
- Stitch 原型使用相同图片或明确的原图占位框，不生成替代商品图。
- 商品图使用 1:1 容器，优先完整展示主体，避免裁掉宠物或商品关键部分。
- 横幅图只用于登录页、工作台欢迎区或大空状态，避免每页重复。
- 侧边栏最多保留一张小型宠物插图，不能挤压导航。
- 空状态可复用现有插图，但需要搭配状态说明和操作按钮。
- 每张商品图预留替代文本。

### 8.4 状态颜色语义

- 品牌主色：用于主要操作、当前导航和关键数字。
- 成功绿色：正常、达标、已完成。
- 预警橙／黄色：建议补货、低库存、待处理。
- 危险红色：断货、失败、不可逆操作。
- 信息蓝色：说明、处理中、虚拟数据提示。
- 中性灰色：禁用、暂无数据、辅助信息。

颜色必须与图标和文字同时使用。

---

## 9. 页面详细需求

### P01 登录与注册

### 页面目标

建立专业、可信、具有宠物行业辨识度的第一印象，并让用户完成登录或员工注册。

### 页面结构

桌面端采用左右分栏：

1. 左侧品牌展示区，占约 52%。
   - 宠物横幅或插图。
   - RetailSense 标志。
   - 一句话定位：“零售选品、定价与库存决策工作台”。
   - 三个能力短标签：选品评分、利润红线、库存预警。
2. 右侧表单区，占约 48%。
   - 语言切换。
   - 登录／注册 Tab。
   - 用户名和密码。
   - 主操作按钮。
   - 版本号和原型说明。

### 交互

- 登录按钮点击后进入局部加载，防止重复提交。
- 密码支持显示／隐藏。
- 登录失败统一提示“用户名或密码错误”。
- 连续失败锁定时显示剩余等待时间。
- 注册用户名实时提示允许字符和 2–32 字符限制。
- 密码提示 8–128 位。
- 注册成功后提示返回登录，不自动暴露密码。

### 页面状态

- 默认登录。
- 注册表单。
- 字段错误。
- 登录处理中。
- 登录失败。
- 会话锁定。

### 移动端

改为单列；插图高度控制在首屏约 28%，表单在其下方。键盘弹出后主按钮仍可触达。

---

### P02 工作台

### 页面目标

让用户快速理解当前数据源、营收、库存风险和下一步可执行任务。

### 页面结构（从上到下）

1. 页面标题区。
   - 标题“工作台”。
   - 当前公司名称。
   - “虚拟公司数据”标签。
   - 数据统计口径说明入口。
2. 欢迎／品牌横幅。
   - 保留宠物图片。
   - 简短欢迎语。
   - 三个快捷操作：开始选品、计算定价、查看库存。
3. 四张 KPI 卡片。
   - 今日营收。
   - 本周营收。
   - 本月营收。
   - 库存零售价值与 SKU 数。
4. 库存健康模块。
   - 正常。
   - 低库存与建议补货合计。
   - 断货。
   - 点击卡片进入库存页并带入筛选。
5. 近期出库。
   - 日期、商品、数量、销售额。
   - 最多显示 5 条。
6. 智能管家。
   - 明确标注“规则式助手”。
   - 示例问题 Chips：查库存、本周营收、利润最高商品、补货建议。
   - 输入框与发送按钮。

### 交互

- 快捷操作跳转对应页面。
- KPI 具有轻量 hover，不产生误导性的可编辑感。
- 公司切换时仅内容区刷新。
- 智能管家回复保留问题和回答历史。
- “今日／本周／本月”提示以虚拟数据最新交易日为锚点。

### 空状态

- 没有公司 JSON 时显示“当前使用内置示例数据”。
- 没有交易时 KPI 不展示误导性趋势，显示“暂无交易记录”。
- 没有库存时显示宠物插图和“检查数据源”按钮。

### 加载状态

- KPI 卡片骨架。
- 库存卡片骨架。
- 近期出库列表骨架。
- 管家回答使用短暂思考状态。

### 移动端

- 横幅单列，快捷操作可横向滚动或两列排列。
- KPI 两列。
- 库存健康三张卡片可横向滑动。
- 近期出库改为列表，不使用五列横排。

---

### P03 案例库

### 页面目标

通过虚拟业务故事解释功能价值，同时避免被理解为真实客户案例。

### 页面结构

1. 标题和醒目的“全部为虚拟演示案例”说明条。
2. 筛选区。
   - 地区。
   - 业务阶段。
   - 行业。
3. 三张案例卡片。
   - 虚拟案例标签。
   - 公司名称、行业、地区、业务阶段。
   - 痛点摘要。
   - 使用功能。
   - 查看详情按钮。
4. 案例详情展开区或右侧抽屉。
   - 使用前。
   - 解决方案。
   - 四项指标对比。
   - 使用商品。
   - “虚拟反馈（演示文案）”。

### 交互

- 筛选后实时更新案例数。
- 查看详情保持列表上下文。
- 每个详情视图再次显示虚拟标签。
- 不出现“真实商家”“客户证言”“实际收益”等未经证实文案。

### 空状态

无匹配案例时显示插图、当前筛选条件和“清除筛选”。

### 移动端

筛选改为顶部筛选按钮；案例卡片单列；指标对比两列。

---

### P04 选品评分

### 页面目标

让用户输入商品条件，理解毛利、竞争、趋势、复购四维得分和最终排名。

### 页面结构

1. 页面标题和“规则评分，不是销量预测”标签。
2. 目标市场选择。
   - 北美、欧洲、东南亚、日韩、澳洲。
   - 展示区域关键词和内置活动信息。
3. 商品选择区。
   - 商品图片、名称、成本、售价。
   - 支持搜索或下拉选择。
4. 四维输入区。
   - 成本／售价。
   - 竞品数量。
   - 搜索增长与趋势方向。
   - 年购买次数与是否消耗品。
5. 计算操作。
6. 评分结果。
   - 最终分数与推荐等级。
   - 四个子分卡片或雷达图。
   - 权重说明：20%、25%、25%、30%。
   - 结果解释。
7. 批量评分排名。
   - 排名、商品、总分、四个子分。
8. 管理员编辑区。
   - 默认折叠。
   - 员工不可见。

### 交互

- 修改任一输入后显示“结果待重新计算”。
- 非法负数直接字段报错。
- 点击子分可查看规则说明。
- 计算完成后平滑滚动到结果区。
- 批量排名支持按总分和子分排序。
- 商品图片必须保留。

### 空状态

未选商品时显示商品插图和“选择商品开始评分”。未计算时保留公式与示例，不显示全零结果。

### 加载状态

按钮显示“计算中”；结果卡片使用短骨架，不需要全屏遮罩。

### 移动端

- 市场选择横向滚动。
- 四维输入单列或两列。
- 子分两列。
- 排名表改为排名卡片，默认只显示总分，点击展开四维详情。

---

### P05 定价模型

### 页面目标

完整展示成本构成，按目标利润率生成最低售价、建议售价和利润结果。

### 页面结构

1. 页面标题和“固定演示汇率”标签。
2. 成本输入卡片。
   - 裸件成本。
   - 加工费。
   - 包装费。
   - 物流费。
   - 平台费金额。
3. 目标利润率滑杆。
4. 币种显示选择。
5. 计算按钮。
6. 成本结构可视化。
   - 五项成本。
   - 总成本。
7. 核心结果卡片。
   - 最低售价。
   - 建议售价。
   - 单件利润。
   - 实际利润率。
   - 是否达到 28% 红线。
8. 公式与口径说明。

### 交互

- 所有金额禁止负数。
- 输入变化后旧结果标记为待更新。
- 售价显示两位小数。
- 红线未达标时显示明确原因，不只显示红色。
- 切换币种只改变展示换算，不改变基础成本数据。
- 显示“汇率为内置演示值，无实时来源”。

### 空状态

未计算时显示计算公式和一个轻量示例。

### 加载与错误

- 计算中禁用重复点击。
- 输入非法时保持用户输入并定位错误字段。

### 移动端

成本输入单列；五项成本可用紧凑列表；结果卡片两列，红线结论独占一行。

---

### P06 销售自动化

### 页面目标

展示 Scout、Price、Copy、Monitor 四段规则流水线及可追踪结果。

### 页面结构

1. 页面标题、“规则引擎”标签和实现边界说明。
2. 运行参数。
   - 目标利润率。
   - 目标市场。
   - 当前商品数量。
3. 四阶段流程图。
   - Scout：评分排序。
   - Price：Top 5 定价。
   - Copy：SEO、社交内容和销售话术。
   - Monitor：库存和利润异常。
4. 启动全流程按钮。
5. 运行进度与日志。
6. 结果 Tab。
   - 评分。
   - 定价。
   - 文案。
   - 监控。
7. 运行摘要。

### 交互

- 运行前显示将处理的商品数和参数。
- 运行时高亮当前阶段，已完成阶段显示完成图标。
- 不使用无法判断进度的长时间转圈。
- 运行完成后默认打开第一个结果 Tab。
- 重新运行时弹窗确认覆盖当前页面结果。
- 日志默认收起，但演示者可展开。

### 页面状态

- 未运行。
- 运行中。
- 完成。
- 某阶段失败。
- 无商品可运行。

### 移动端

四阶段改为纵向时间线；结果 Tab 可横向滚动；文案内容使用折叠卡片。

---

### P07 库存监控

### 页面目标

快速发现断货、低库存和已到补货点的 SKU，并查看补货建议。

### 页面结构

1. 页面标题、公司数据源和更新时间。
2. 状态摘要。
   - 正常。
   - 建议补货。
   - 低库存。
   - 断货。
3. 搜索与筛选。
   - 商品／SKU 搜索。
   - 状态筛选。
   - 可选排序：库存量、周转天数、补货优先级。
4. 库存表格。
   - 商品图。
   - 商品名。
   - SKU。
   - 当前库存。
   - 日均销量。
   - 周转天数。
   - 安全库存。
   - 补货点。
   - 建议补货量。
   - 状态。
5. 状态公式说明。

### 交互

- 点击摘要卡片筛选对应状态。
- 四个状态必须互斥，数量合计等于 SKU 总数。
- 点击商品行打开详情抽屉。
- 商品详情展示图片、核心数字和计算说明。
- 支持清除筛选。
- 无日均销量时显示“暂不能计算周转”，不能显示除零错误。

### 空状态

- 完全无库存：插图、说明和检查数据源按钮。
- 筛选无结果：显示筛选条件和清除筛选。

### 加载状态

摘要和表格分别显示骨架；切换筛选不使用全页加载。

### 移动端

表格改为商品卡片：图片、商品名、SKU、库存、状态、补货量为首层；其余数据展开查看。

---

### P08 商品上架

### 页面目标

员工选择商品和平台，检查 Listing 并完成模拟上架及本地记录。

### 权限状态

- 员工：正常进入。
- 管理员：显示无权限页面，解释“上架操作由员工角色执行”，提供返回工作台按钮。

### 页面结构

1. 页面标题和“模拟上架／不调用真实平台 API”提示。
2. 两个 Tab。
   - 上架操作。
   - 上架记录。
3. 商品选择网格。
   - 商品图。
   - 商品名称。
   - 成本、售价。
   - 待上架／已上架状态。
4. 选中商品详情。
5. 平台选择。
   - Shopify。
   - Etsy。
   - 独立站。
6. Listing 预览。
   - 标题。
   - 卖点／描述。
   - SEO 标签。
   - 价格。
7. 确认模拟上架。
8. 结果反馈。
9. 上架记录列表。

### 交互

- 选中商品卡片有明确边框和选中标记。
- 已上架商品不可重复操作，提供查看记录入口。
- 上架前弹出确认框，列出商品、平台和价格。
- 成功文案必须是“模拟上架成功，已保存本地记录”。
- 移除记录属于改变状态操作，需要确认。
- 商品图片优先完整显示。

### 状态

- 无商品。
- 商品未选择。
- 待上架。
- 上架处理中。
- 模拟上架成功。
- 本地记录保存失败。
- 管理员无权限。

### 移动端

商品两列网格；详情和 Listing 预览纵向排列；确认按钮可在底部形成固定操作区，但不能遮挡内容。

---

### P09 物流配发

### 页面目标

员工查看虚拟订单，完成配货和虚拟发货，并查看六阶段物流轨迹。

### 权限状态

- 员工：正常操作。
- 管理员：显示角色说明和返回入口。

### 页面结构

1. 页面标题和“虚拟订单／模拟物流”提示。
2. 订单状态摘要。
   - 待处理。
   - 拣货中。
   - 已发货。
   - 已完成／已签收。
3. 搜索、状态筛选和分页。
4. 订单列表。
   - 订单号。
   - 创建时间。
   - 虚拟客户。
   - 商品摘要。
   - 数量。
   - 金额。
   - 状态。
   - 主要操作。
5. 行内展开或右侧详情抽屉。
   - 收货信息标记为虚拟。
   - SKU 拣货清单。
   - 仓库位置。
   - 库存是否充足。
6. 配货结果。
7. 虚拟发货确认。
8. 六阶段物流时间线。
   - 已接单。
   - 配货完成。
   - 已出库。
   - 运输中。
   - 派送中。
   - 已签收。
9. 仓库库存概览。

### 交互

- 摘要卡片可筛选订单。
- 配货前显示每个 SKU 的需求量和库存量。
- 缺货时列出具体 SKU 和缺少数量。
- 虚拟发货前二次确认。
- 运单号明确标注为模拟编号。
- 物流动画允许跳过或刷新，不锁死整个页面。
- 订单状态更新后保留当前筛选和页码。

### 空状态与错误

- 无订单：宠物物流插图和说明。
- 筛选无结果：清除筛选。
- 库存不足：错误详情和返回修改入口。
- 轨迹生成失败：保留已完成的配货结果并允许重试。

### 移动端

订单表改为卡片；详情使用底部抽屉或新页面；六阶段时间线纵向显示。

---

### P10 导出报表

### 页面目标

让用户查看当前数据摘要，选择导出范围并安全下载 CSV 或文本文件。

### 页面结构

1. 页面标题、公司名称和“虚拟演示数据”标签。
2. 公司信息摘要。
3. 四张数据概览卡片。
4. 产品列表预览。
5. 近期交易预览。
6. 导出内容选择。
   - 完整报告。
   - 仅产品列表。
   - 仅营收汇总。
7. 下载格式。
   - CSV。
   - 文本。
8. 导出内容预览。
9. 数据口径说明。

### 交互

- 选择导出范围后更新预览摘要。
- 下载前显示公司、统计范围和格式。
- 文件生成中禁用重复点击。
- 成功后显示轻量 Toast。
- 文件内需带“虚拟演示数据”说明。
- 无数据时禁用下载按钮。

### 移动端

预览表格默认折叠；格式按钮整行宽度；数据卡片两列。

---

### P11 设置、关于与更新日志

### 页面形式

桌面端建议使用侧边抽屉或独立设置页；移动端使用全屏设置页面。

### 模块结构

1. 显示与语言。
   - 中文／英文。
   - 字号设置。
2. 虚拟公司数据。
   - 是否启用公司数据。
   - 三家虚拟公司选择。
   - 明确虚拟标记。
3. 图片管理，仅管理员。
   - 当前商品图缩略图。
   - 上传替换图。
   - 文件大小和格式说明。
   - 恢复默认。
4. 平台配置，仅管理员。
   - Shopify、Etsy、独立站和物流配置。
   - 密钥字段隐藏。
   - “仅保存在本地，不代表已接通 API”提示。
5. 关于。
   - 产品定位。
   - 当前能力边界。
6. 更新日志。
   - 当前版本 v2.5.1。
   - 历史版本列表。

### 交互

- 有未保存修改时关闭抽屉需提示。
- 图片上传校验 JPG、PNG、WebP 和 5MB 限制。
- 恢复默认图需要确认。
- 配置保存成功显示本地保存提示。
- 员工不显示管理员设置项。

---

## 10. 关键流程与原型连线

### F01 新品决策流程

```text
工作台
→ 点击“开始选品”
→ 选品评分
→ 选择市场和商品
→ 输入四维参数
→ 计算评分
→ 查看批量排名
→ 点击“进入定价”
→ 定价模型
→ 输入完整成本
→ 查看建议售价与红线
```

### F02 库存风险流程

```text
工作台低库存卡片
→ 库存监控（自动带入风险筛选）
→ 选择建议补货商品
→ 打开商品详情
→ 查看安全库存、补货点和建议补货量
```

### F03 员工执行流程

```text
商品上架
→ 选择商品
→ 选择平台
→ 查看 Listing
→ 确认模拟上架
→ 上架记录
→ 物流配发
→ 选择待处理订单
→ 配货
→ 虚拟发货
→ 查看六阶段轨迹
```

### F04 面试演示流程

```text
登录
→ 工作台概览
→ 选品评分
→ 定价模型
→ 销售自动化
→ 库存监控
→ 案例库虚拟边界
→ 更新日志／项目总结
```

Stitch 原型必须为以上路径建立可点击连线。

---

## 11. 全局页面状态规范

### 11.1 空状态

每个空状态包含：

1. 现有宠物插图或统一占位图。
2. 明确标题，例如“暂无库存数据”。
3. 一句原因或说明。
4. 一个主要操作，例如“检查数据源”。
5. 必要时提供次要操作“使用示例数据”。

不能只显示空白表格或“暂无数据”四个字。

### 11.2 加载状态

- 首次加载使用骨架屏。
- 局部筛选使用局部加载。
- 计算操作使用按钮加载。
- 四阶段流水线使用阶段进度。
- 超过 10 秒的模拟动画必须显示进度或允许跳过。
- 加载时保留页面结构，避免布局跳动。

### 11.3 错误状态

错误提示需要包含：

- 发生了什么。
- 用户输入是否保留。
- 下一步操作：重试、返回或修改。

不要只显示错误代码。

### 11.4 成功状态

- 小操作使用 Toast。
- 重要流程使用结果卡片。
- 模拟上架和物流必须在成功文案中保留“模拟”字样。

### 11.5 无权限状态

- 显示锁图标、页面名称和当前角色。
- 解释该操作属于哪个角色。
- 提供返回工作台按钮。
- 不展示技术错误或空白页面。

### 11.6 确认弹窗

以下操作需要确认：

- 模拟上架。
- 移除上架记录。
- 虚拟发货。
- 恢复默认图片。
- 放弃未保存的设置。

---

## 12. 移动端适配规范

### 12.1 导航

- 使用顶部菜单按钮打开导航抽屉。
- 抽屉中保留完整九个页面入口和分组。
- 顶部显示页面名称、数据源简写和关键操作。
- 不建议把九个页面全部塞进底部导航。

### 12.2 布局

- 单列优先。
- KPI 通常两列，极窄屏单列。
- 表格优先转卡片，无法转化时才横向滚动。
- 弹窗在手机端接近全屏。
- 主要按钮点击区域不小于约 44px。
- 页面底部固定按钮需要预留安全区域。

### 12.3 内容优先级

手机端首层优先显示：

- 页面标题。
- 当前数据源。
- 关键数字。
- 状态与风险。
- 当前主要操作。

公式、日志、长说明和次要字段默认折叠。

### 12.4 必须输出手机稿的页面

1. 登录／注册。
2. 工作台。
3. 选品评分。
4. 定价模型。
5. 库存监控。
6. 商品上架。
7. 物流订单与详情。

其余页面至少需要在桌面稿中体现响应式设计原则。

---

## 13. 中英文与可访问性

### 13.1 中英文

- 所有按钮为英文长文本预留空间。
- 公司名、商品名和状态标签允许合理换行。
- 中英文切换不改变页面结构。
- 金额、日期和百分比格式保持一致。

### 13.2 可访问性

- 正文与背景满足清晰对比度。
- 键盘焦点可见。
- 输入框具有可见标签，不只依赖 placeholder。
- 状态不只用颜色。
- 商品图片具有替代文字。
- 表单错误与字段建立明确关系。
- 动画避免快速闪烁。

---

## 14. 原型数据要求

Stitch 原型应使用接近现有项目的数据，避免无意义的 Lorem Ipsum。

### 14.1 示例公司

- 萌爪宠物用品（杭州）。
- PawStyle Studio（上海）。
- Bark & Co.（加州）。

所有名称旁需在适当位置标注虚拟数据。

### 14.2 示例商品

- 刻字狗牌。
- 发光项圈。
- 珐琅名牌。
- 牵引绳套装。
- 换牙零食。
- 亚克力牌。
- 宠物领结。
- 宠物手链。

使用对应现有商品图片。

### 14.3 定价默认值

- 裸件成本：2.80。
- 加工费：1.20。
- 包装费：0.50。
- 物流费：1.50。
- 平台费：0.85。
- 总成本：6.85。
- 目标利润率：45%。
- 建议售价：12.46。
- 单件利润：5.61。

这些数据用于视觉原型展示，不要求 Stitch 实现计算。

### 14.4 库存状态

原型至少展示：

- 一个正常商品。
- 一个建议补货商品。
- 一个低库存商品。
- 一个断货商品。
- 一个无日均销量、暂不能计算周转的商品状态。

---

## 15. Stitch 交付页面清单

### 15.1 P0：必须完成的桌面主稿

| Frame ID | 页面 | 必须展示的状态 |
|---|---|---|
| D01 | 登录 | 默认登录 |
| D02 | 工作台 | 虚拟公司已接入 |
| D03 | 案例库 | 三张虚拟案例卡片 |
| D04 | 选品评分 | 已计算结果与排名 |
| D05 | 定价模型 | 已计算并达标 |
| D06 | 销售自动化 | 四阶段运行完成 |
| D07 | 库存监控 | 四种库存状态 |
| D08 | 商品上架 | 商品与 Listing 预览 |
| D09 | 物流配发 | 订单列表 |
| D10 | 导出报表 | 完整报告模式 |
| D11 | 设置 | 管理员设置抽屉／页面 |

### 15.2 P0：关键状态稿

| Frame ID | 状态 |
|---|---|
| S01 | 工作台加载中 |
| S02 | 工作台无数据 |
| S03 | 销售自动化运行中 |
| S04 | 商品模拟上架确认 |
| S05 | 商品模拟上架成功 |
| S06 | 物流订单配货详情与库存不足 |
| S07 | 管理员进入员工专属页面的无权限状态 |

### 15.3 P1：手机稿

| Frame ID | 页面 |
|---|---|
| M01 | 登录／注册 |
| M02 | 工作台 |
| M03 | 选品评分 |
| M04 | 定价模型 |
| M05 | 库存监控 |
| M06 | 商品上架 |
| M07 | 物流订单详情 |

### 15.4 原型连线

- 左侧导航可进入全部主页面。
- 工作台快捷操作可进入选品、定价和库存。
- 选品结果可进入定价。
- 商品上架可在操作与记录 Tab 间切换。
- 物流订单可展开详情、执行配货并进入轨迹。
- 设置可打开、修改、保存或放弃。
- 关键确认弹窗具备取消与确认动作。

---

## 16. 文案规范

### 推荐文案

- “虚拟公司数据”。
- “虚拟业务案例”。
- “模拟上架”。
- “已保存本地记录”。
- “模拟物流轨迹”。
- “规则式助手”。
- “内置演示汇率”。
- “评分用于辅助决策，不代表销量预测”。

### 禁止文案

- “真实客户案例”。
- “实时汇率”。
- “已成功发布到 Shopify／Etsy”。
- “真实快递追踪”。
- “大模型已自动决策”。
- “企业级权限系统”。
- “保证销量／保证利润”。

---

## 17. 最终验收标准

### 功能完整性

- 九个主导航页面均有完整桌面稿。
- 登录与设置构成完整辅助页面。
- 页面模块与本 PRD 一致，没有因视觉改版删除核心字段。
- 四条关键流程全部可点击演示。

### 业务准确性

- 评分明确为四个维度。
- 定价包含五项成本和 28% 红线。
- 库存包含安全库存、补货点和建议补货量。
- 四阶段流程明确为规则和模板。
- 虚拟数据、模拟上架和模拟物流标识完整。

### 视觉一致性

- 使用统一导航、卡片、表格、按钮、表单和状态组件。
- 参考风格在所有页面保持一致。
- 宠物图片和插图均来自现有资产或使用原图占位。
- 数据可读性优先于装饰。

### 状态完整性

- 核心页面具备空、加载、错误或成功状态。
- 员工专属页面具备管理员无权限状态。
- 危险和改变记录的操作具备确认弹窗。

### 响应式

- 7 个关键手机页面完成。
- 手机端不依赖 hover。
- 表格和长流程有明确移动端替代布局。
- 主要操作在单手范围内可触达。

---

## 18. 给 Stitch 的最终摘要

请设计一个温暖、现代、清晰的宠物零售 SaaS 原型。它不是电商商城，而是零售运营决策工具。页面需要同时具备数据专业感和宠物行业亲和力。

重点突出三条产品主线：

1. 决策：选品评分与定价。
2. 风险：库存预警与补货。
3. 执行：模拟上架与模拟履约。

保留并复用现有宠物图片。不要用装饰性视觉压缩功能模块，不要删除表格和状态，不要把模拟能力设计成真实平台能力。桌面稿应适合完整业务演示，手机稿应保证关键任务可完成。
